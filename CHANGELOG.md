# Changelog

## v0.1.3

- File discovery now follows `.gitignore` rules.

## v0.1.2

- `-L` and `--languages` now support language extension input (for example, `.rs`).

## v0.1.1

- Added `-X` and `--exclude` for excluding paths from scans.

## v0.1.0

- Initial release.
